package com.pfe.sageline.enums;

public enum ConversationType {
    LINE_ASSIGNMENT,      // Affectation à une ligne
    VALIDATION_ASSIGNMENT, // Affectation à une validation
    DIRECT                // Message direct
}